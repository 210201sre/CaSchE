apiVersion: v1
kind: Service
metadata:
  name: project-two
  namespace: casche
spec:
  selector:
    app: project-two
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
